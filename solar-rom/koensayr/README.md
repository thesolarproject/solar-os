# Koensayr (AVRCP patches)

Clone the upstream patcher here before building a ROM with full AVRCP support:

```bash
git clone https://github.com/SeanathanVT/koensayr.git .
```

CI checks out this repo automatically. Local builds can also set `KOENSAYR_DIR=/path/to/koensayr`.
