<?xml version="1.0" encoding="utf-8"?>
<!-- 2026-07-05 — Status bar strip for system IME overlay (matches MainActivity layout_status_bar). -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/layout_ime_status_bar"
    android:layout_width="match_parent"
    android:layout_height="@dimen/y1_status_bar_height"
    android:paddingLeft="14dp"
    android:paddingRight="14dp"
    android:background="#00000000">

    <TextView
        android:id="@+id/tv_ime_status_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_toLeftOf="@+id/ime_status_bar_icons"
        android:layout_marginRight="8dp"
        android:layout_centerVertical="true"
        android:singleLine="true"
        android:ellipsize="marquee"
        android:marqueeRepeatLimit="marquee_forever"
        android:scrollHorizontally="true"
        android:textColor="#FFFFFF"
        android:textSize="18sp"
        android:textStyle="bold" />

    <LinearLayout
        android:id="@+id/ime_status_bar_icons"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentRight="true"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/tv_ime_status_battery"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="100%"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:textStyle="bold" />
    </LinearLayout>
</RelativeLayout>
