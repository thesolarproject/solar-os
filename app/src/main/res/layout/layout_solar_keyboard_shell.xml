<?xml version="1.0" encoding="utf-8"?>
<!-- 2026-07-05 — Full-screen keyboard shell: theme backdrop + status bar + wheel strip. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/layout_solar_keyboard_shell"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/iv_keyboard_shell_bg"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:src="@drawable/default_back" />

    <ImageView
        android:id="@+id/iv_keyboard_shell_mask"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:visibility="gone" />

    <View
        android:id="@+id/view_keyboard_shell_tint"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#00000000" />

    <RelativeLayout
        android:id="@+id/layout_keyboard_status_bar"
        android:layout_width="match_parent"
        android:layout_height="@dimen/y1_status_bar_height"
        android:layout_gravity="top"
        android:paddingLeft="14dp"
        android:paddingRight="14dp"
        android:background="#00000000">

        <TextView
            android:id="@+id/tv_status_clock"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentLeft="true"
            android:layout_centerVertical="true"
            android:singleLine="true"
            android:ellipsize="marquee"
            android:marqueeRepeatLimit="marquee_forever"
            android:scrollHorizontally="true"
            android:textSize="18sp"
            android:textStyle="bold" />
    </RelativeLayout>

    <include
        android:id="@+id/layout_solar_wheel_keyboard"
        layout="@layout/layout_solar_wheel_keyboard"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="@dimen/y1_status_bar_height" />
</FrameLayout>
